#include <bits/stdc++.h>
using namespace std;

vector<int> table;
unordered_set<char> operators({'+', '=', '-', '*', '/', '%'});
unordered_set<char> special_symbols({';', ',', '(', ')', '$', '!', '@'});
unordered_map<char, int> addresses;

int main() {
  return 1;
  return 0;
}